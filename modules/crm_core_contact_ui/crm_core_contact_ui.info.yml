name: CRM Core Contact UI
type: module
description: 'Provides a user interface for CRM Core Contact.'
package: CRM Core
version: 8.x-1.x-dev
core: 8.x

configure: crm_core_contact_ui.contact_types

dependencies:
  # Core dependencies
  - crm_core_contact
  - crm_core_ui
  - search

  # Contrib
  # @todo Readd once awaillable and still needed.
#  - views_bulk_operations

stylesheets:
  all:
    - crm_core_contact_ui.css

